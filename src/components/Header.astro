<header class="header">
  <nav class="nav">
    <a href="/" class="logo">dreamnb<span class="logo-suffix"> 有梦想，就是nb</span></a>
    <div class="nav-right">
      <a href="/gallery" class="nav-btn">作品欣赏</a>
      <a href="/video" class="nav-btn">视频欣赏</a>
    </div>
  </nav>
</header>

<style>
  .header {
    border-bottom: 1px solid var(--color-border);
    background: var(--color-bg);
  }

  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: var(--content-width);
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 56px;
  }

  .logo {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-text);
    text-decoration: none;
  }

  .logo:hover {
    color: var(--color-accent);
    text-decoration: none;
  }

  .logo-suffix {
    font-weight: 400;
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .nav-right {
    flex-shrink: 0;
  }

  .nav-right {
    display: flex;
    gap: 8px;
  }

  .nav-btn {
    display: inline-block;
    padding: 6px 16px;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--color-accent);
    border: 1px solid var(--color-accent);
    border-radius: 6px;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .nav-btn:hover {
    color: #fff;
    background: var(--color-accent);
    text-decoration: none;
  }
</style>